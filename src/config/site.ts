/* eslint-disable prettier/prettier */

export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Randmlist",
  description: "Una lista de selecciones aleatorias",
  navItems: [
    {
      label: "Inicio",
      href: "/",
    }
  ],
  links: {
  },
};
